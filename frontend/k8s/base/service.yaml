apiVersion: v1
kind: Service
metadata:
  name: renthub-frontend
  labels:
    app: renthub-frontend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: renthub-frontend
