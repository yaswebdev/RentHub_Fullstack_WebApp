apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - configmap.yaml
  - ingress.yaml
  - hpa.yaml
  - service.yaml

labels:
  - pairs:
      app: renthub-backend
      part-of: renthub
