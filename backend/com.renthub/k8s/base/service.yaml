apiVersion: v1
kind: Service
metadata:
  name: renthub-backend
  labels:
    app: renthub-backend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: renthub-backend
